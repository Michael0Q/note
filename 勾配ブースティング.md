前のモデルの間違いを、損失関数の出力から偏微分して算出し、その偏微分係数の符号を反転させて「擬似残差」を得る。その擬似残差を予測する新しいモデルを作成し、追加していく。

### 結論

なので、最終的なモデルの予測は、すべてのモデルの足し算になる。

$$  
F_M(x) = F_0(x) + \sum_{m=1}^{M} \nu\, h_m(x)  
$$

- F_M＝最終的なモデル
- F_0＝最初のモデル
- v=重み
- h_m=これまでのモデル（m-1）の擬似残差を学習したモデル

### 勾配を求めるステップ

#### 損失関数の定義・損失の求め方

これを最小化するのを目的として、勾配を計算する。

- **回帰の場合**
    
    **2乗誤差を損失と定義する。**
    
    $$  
    L(y, F) = \tfrac{1}{2}(y - F)^2  
    $$
    
- **2値分類の場合**
    
    これは「＋」を軸に第一項と第二項の2つで考えられる。y∈(0,1)なので、y＝0なら、左辺は消える。  
    y=1なら右辺が消える。（0との掛け算になり、logが消える）
    
    $$  
    L(y, F) = -\big[y \log \sigma(F) + (1-y)\log(1-\sigma(F))\big]  
    $$
    

1. **Fを求める。**
    
    まず最初のモデルの出力である。最初のモデルは、全体の陽性率を陰性率を割ったものを対数変換することで求められる。これを[[対数オッズ]]と呼ぶ。
    
    $$  
    F_0 = \log \frac{\bar{p}}{1 - \bar{p}}  
    $$
2. **シグモイド関数にかける**
	[[シグモイド関数]]
    

$$  
\sigma(F) = \frac{1}{1 + e^{-F}}  
$$


3. **シグモイド関数の結果の自然対数を取る**

ここまでで、log σ（F）まできた。上の例に倣い、σ＝0.98とすると

log σ（F）= -0.0202027073　となる。これに外のマイナスをかけて

L（y, F）= 0.0202027073となる。マイナスをかけるのは、log σが常に-の値になるため。

これが損失関数の「損失」となり、これを最小化するのが目的となる。

### 損失関数について偏微分し、最適化の方向を導出する

結論としてはこう。

$$  
-\frac{\partial L}{\partial F} = y - \sigma(F)  
$$

∂は偏微分の記号。Lという関数のFを微分するという宣言、合図のようなもの。下記にこれから偏微分のプロセスを書くが、最終的に y - σ(F) と一致する、という美しい定理がある。

#### シグモイド関数の微分をする

本文で2回登場するため、先に微分しておく。結論として下記になる。

$$  
\frac{d}{dF}\sigma(F) = \sigma(F)\big(1 - \sigma(F)\big) = P(1 - P)  
$$

シグモイド関数は下記左辺からなるため、[[指数法則]]を活用し下記右辺の形に展開する。

$$  
\frac{1}{1 + e^{-F}} = (1 + e^{-F})^{-1}  
$$

これ自体もさらに微分するので、内側外側分けて考える。

まず外側は (1＋e^F)^-1を微分して下記。 (1＋e^F) = uと仮定。

$$  
-u^{-2}  
$$

内側は定数1が微分で消えて、-が前に降りる。

$$  
-e^{-F}  
$$

それぞれかけて下記。なお指数を分母に戻すところは[[指数法則]]を活用。

$$  
\begin{aligned}  
&\text{外側の微分 × 内側の微分（連鎖律）} \\[4pt]  
& -(1 + e^{-F})^{-2} \cdot (-e^{-F}) \\[10pt]  
&\text{（-1）×（-1）= +1 でマイナスが消える} \\[4pt]  
&= (1 + e^{-F})^{-2} \cdot e^{-F} \\[10pt]  
&\text{指数を分母に戻す} \\[4pt]  
&= \frac{e^{-F}}{(1 + e^{-F})^2}  
\end{aligned}  
$$

これがシグモイド関数について微分した結果になる。

以下より、結論のP(1-P)になる証明を行う。

$$  
\frac{e^{-F}}{(1 + e^{-F})^2}  
$$

分母が2乗なので、$a^2 = a \cdot a$ により2つの分数の積に分解できる。  
このとき $P = \dfrac{1}{1 + e^{-F}} = \sigma(F)$（分子が1のやつ）。

$$  
\frac{e^{-F}}{(1 + e^{-F})^2}  
= \frac{e^{-F}}{1 + e^{-F}} \cdot \frac{1}{1 + e^{-F}}  
= \frac{e^{-F}}{1 + e^{-F}} \cdot P  
$$

次に左側の分数を変形する。ただの足し引きで、$e^{-F}$ に $1$ を足してから $1$ を引いても値は変わらない。

$$  
e^{-F} = e^{-F} + 1 - 1 = (1 + e^{-F}) - 1  
$$

こうする理由は、分母が $1 + e^{-F}$ なので、分子を「分母と同じかたまり $(1+e^{-F})$」と「残り $-1$」に分けたいから。そうすると分数が割れて $\dfrac{1}{1+e^{-F}} = P$ が取り出せる。この形が1-Pになる。

$$  
\frac{(1 + e^{-F}) - 1}{1 + e^{-F}}  
= \frac{1 + e^{-F}}{1 + e^{-F}} - \frac{1}{1 + e^{-F}}  
= 1 - P  
$$

これを戻すと、全体は次のようになる。

$$  
\frac{e^{-F}}{1 + e^{-F}} \cdot P = (1 - P) \cdot P = P(1 - P)  
$$

よって、σ(F)の微分は下記のようにP(1-P)と表現できる。（Pが分かれば微分できる。）

$$  
\frac{d}{dF}\sigma(F) = \sigma(F)\big(1 - \sigma(F)\big) = P(1 - P)  
$$

### **結果を全体式に代入する**

log loss を $F$ で偏微分する（各パーツ）

$$  
L(y, F) = -\big[y \log \sigma(F) + (1-y)\log(1-\sigma(F))\big]  
$$

**第1項 $y \log \sigma(F)$ の中身**

$$  
\begin{aligned}  
&\log P \text{ を } P \text{ で微分} &&\rightarrow \frac{1}{P} \\[6pt]  
&\sigma(F) \text{ を } F \text{ で微分} &&\rightarrow P(1-P) \\[6pt]  
&\text{連鎖律で掛ける} &&\rightarrow \frac{1}{P} \cdot P(1-P) = 1 - P  
\end{aligned}  
$$

**第2項 $(1-y)\log(1-\sigma(F))$ の中身**

$$  
\begin{aligned}  
&\log(1-P) \text{ を } (1-P) \text{ で微分} &&\rightarrow \frac{1}{1-P} \\[6pt]  
&1 - \sigma(F) \text{ を } F \text{ で微分} &&\rightarrow -P(1-P) \\[6pt]  
&\text{連鎖律で掛ける} &&\rightarrow \frac{1}{1-P} \cdot \big(-P(1-P)\big) = -P  
\end{aligned}  
$$

**全体を組み立てる**

上記の結果を各項に代入し、 $y$、$1-y$ を掛けて、先頭のマイナスを付けて足し合わせる。

$$  
\begin{aligned}  
\frac{\partial L}{\partial F}  
&= -\big[\, y \cdot (1 - P) + (1-y)\cdot(-P) \,\big] \\[6pt]  
&= -\big[\, y - yP - P + yP \,\big] &&\text{（展開すると } yP \text{ が打ち消し合う）} \\[6pt]  
&= -\big[\, y - P \,\big] \\[6pt]  
&= P - y  
\end{aligned}  
$$

**擬似残差**

勾配ブースティングでは負の勾配を擬似残差とするので、

$$  
r = -\frac{\partial L}{\partial F} = y - P = y - \sigma(F)  
$$

つまり擬似残差は「正解ラベル $y$ と予測確率 $\sigma(F)$ の差」になる。
擬似残差はのちのテイラー展開を行う時のベースになる。

### 擬似残差を元にモデルを学習
https://claude.ai/chat/59c6f643-b891-40fe-8526-ecd2c0cc6218　のStep1
**損失関数をテイラー展開で近似して、修正するべき量を求める**
残差だけだと、どれくらいその方向に進めばいいか曖昧なので、2次のテイラー展開で曲率を求める。曲率にしてあげることで、頂点（底）に向かって、どれくらい大きく進めばいいかがわかる。
![[テイラー展開]]

これを今回の例に当てはめると、下記のようになる。
$$
L\big(y_i,\, F_{m-1,i} + f_t(x_i)\big)
\approx L\big(y_i,\, F_{m-1,i}\big) + g_i f_t(x_i) + \frac{1}{2} h_i f_t(x_i)^2
$$
左辺は損失関数で、一つ前の木に今回の木の予測を足すことで新しい損失がもとまるという意味。
右辺が近似式で、損失関数をテイラー展開で2次近似まで求め、足している。
つまり、これまでのモデルに対して新しい木の出力を足した損失関数は、これまでのモデルに対して新しい木の出力を足した損失関数に、