<%*
const title = await tp.system.prompt("問題名（例: 2021年6月_問3_パーシェ指数の計算）");
if (!title) {
	tR += "（キャンセルされました。このノートは削除してください）";
	return;
}
// 既存問題の分野を収集し、検索絞り込み付きの選択リストで入力する
const catSet = new Set();
for (const f of app.vault.getMarkdownFiles()) {
	if (!f.path.startsWith("exam/問題/")) continue;
	const c = app.metadataCache.getFileCache(f)?.frontmatter?.category;
	if (c) catSet.add(String(c));
}
const cats = [...catSet].sort((a, b) => a.localeCompare(b, "ja"));
const NEW_CAT = "＋ 新規入力…";
let category = "";
if (cats.length > 0) {
	const picked = await tp.system.suggester(
		[...cats, NEW_CAT],
		[...cats, NEW_CAT],
		false,
		"分野を選択（入力で絞り込み）"
	);
	if (picked === NEW_CAT) {
		category = (await tp.system.prompt("分野（例: 民法/債権）")) ?? "";
	} else {
		category = picked ?? "";
	}
} else {
	category = (await tp.system.prompt("分野（例: 民法/債権）")) ?? "";
}
await tp.file.move("exam/問題/" + title);
-%>
---
tags: [problem]
category: "<% category %>"
level: 0
last: <% tp.date.now("YYYY-MM-DD") %>
status: ""
retired: false
---

```meta-bind-button
id: srs-o
class: srs-btn-o
hidden: true
label: "○ 正解＋説明できた"
style: primary
action:
  type: js
  file: exam/スクリプト/review.js
  args:
    grade: "○"
```

```meta-bind-button
id: srs-tri
class: srs-btn-tri
hidden: true
label: "△ 正解したが説明できない"
style: default
action:
  type: js
  file: exam/スクリプト/review.js
  args:
    grade: "△"
```

```meta-bind-button
id: srs-x
class: srs-btn-x
hidden: true
label: "× 不正解"
style: destructive
action:
  type: js
  file: exam/スクリプト/review.js
  args:
    grade: "×"
```

`BUTTON[srs-o]` `BUTTON[srs-tri]` `BUTTON[srs-x]`

## 回答プロセス


## 復習メモ
