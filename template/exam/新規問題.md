<%*
const title = await tp.system.prompt("問題名（例: 2021年6月_問3_パーシェ指数の計算）");
if (!title) {
	tR += "（キャンセルされました。このノートは削除してください）";
	return;
}
// 分野は統計検定2級 出題範囲表（2018/12/14版）の小項目から選ぶ。
// 既存ノートのcategoryを候補にすると、一度混入した誤記（過去に「検定」が4件汚染）が
// 候補として再利用され揺れが固定化するため、範囲表の固定リストだけを出す。
// https://www.toukei-kentei.jp/hubfs/files/grade_range/grade2_hani_20181214.pdf
const SUBCATEGORIES = [
	"身近な統計",
	"データの分布の記述",
	"中心傾向の指標",
	"散らばりなどの指標",
	"中心と散らばりの活用",
	"散布図と相関",
	"カテゴリカルデータ",
	"単回帰と予測",
	"時系列データの処理",
	"観察研究と実験研究",
	"標本調査と無作為抽出",
	"実験",
	"確率",
	"確率変数",
	"確率分布",
	"標本分布",
	"推定",
	"仮説検定",
	"回帰分析",
	"実験計画の概念の理解",
	"統計ソフトウェアの活用",
];
const EXAM = "統計検定2級";
const cats = SUBCATEGORIES.map((s) => EXAM + "/" + s);
const OTHER_CAT = "＋ 他の試験・分野を手入力…";
const picked = await tp.system.suggester(
	[...cats, OTHER_CAT],
	[...cats, OTHER_CAT],
	false,
	"分野を選択（出題範囲表の小項目。入力で絞り込み）"
);
let category = "";
if (picked === OTHER_CAT) {
	// 統計検定2級以外の試験を扱う場合のみ使う。2級の問題では上のリストから選ぶこと
	category = (await tp.system.prompt("分野（例: 統計検定準1級/多変量解析）")) ?? "";
} else {
	category = picked ?? "";
}
await tp.file.move("exam/問題/" + title);
-%>
---
tags: [problem]
category: "<% category %>"
level: 0
last: <% tp.date.now("YYYY-MM-DD") %>
status: ""
retired: false
---

```meta-bind-button
id: srs-o
class: srs-btn-o
hidden: true
label: "○ 正解＋説明できた"
style: primary
action:
  type: js
  file: exam/スクリプト/review.js
  args:
    grade: "○"
```

```meta-bind-button
id: srs-tri
class: srs-btn-tri
hidden: true
label: "△ 正解したが説明できない"
style: default
action:
  type: js
  file: exam/スクリプト/review.js
  args:
    grade: "△"
```

```meta-bind-button
id: srs-x
class: srs-btn-x
hidden: true
label: "× 不正解"
style: destructive
action:
  type: js
  file: exam/スクリプト/review.js
  args:
    grade: "×"
```

`BUTTON[srs-o]` `BUTTON[srs-tri]` `BUTTON[srs-x]`

## 回答プロセス


## 復習メモ
