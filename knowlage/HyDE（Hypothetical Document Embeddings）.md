[[RAG]]などで用いられる、精度改善手法の一つ。質問や、既存のデータセットから、仮のデータを作成し、そのデータのベクトルを用いて文書検索などを行う。

例えば、ユーザーのクエリとドキュメントの形式が違うと、埋め込みとして検索するのが難しいという課題がある。理由としてはユーザーの入力がドキュメントの形式と一致しないので、類似度が近くならない。
そこで、ユーザーのクエリから、理想の回答などをLLMに生成させ、その回答を埋め込み化することで、ドキュメントのベクトル空間に写像して精度の良い検索ができるといったもの。

