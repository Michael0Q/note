# 問題演習ダッシュボード

```dataviewjs
// ===== 設定 =====
const LADDER = [1, 2, 3, 5, 8, 13, 21, 34, 55]; // 復習間隔ラダー（日）
const QUEUE_WARN = 10; // 今日のキューがこの件数以上なら新規追加を控える目安
const SRC = '("exam/問題" or "knowlage") and #problem'; // テンプレート混入防止のためフォルダ限定 + #problem 必須

// ===== 日付ユーティリティ =====
// dv.date("today") と frontmatter の日付はどちらもローカルタイムゾーンの0時として
// パースされるが、文字列("2026-08-05" と引用符付きで書かれた場合)や時刻付きの値が
// 混ざっても壊れないよう、常に startOf("day") に正規化してから比較する。
const today = dv.date("today").startOf("day");

function asDay(v) {
	if (!v) return null;
	const d = typeof v === "string" ? dv.date(v) : v;
	return d && d.startOf ? d.startOf("day") : null;
}

// 次回復習日は保存せず、ここで常に last + LADDER[level] を計算する。
// ただし status="×" は「翌日必ず出す」ため last + 1日 に上書きする。
function dueOf(p) {
	const last = asDay(p.last);
	if (!last) return null;
	let lvl = Number(p.level);
	if (!Number.isFinite(lvl)) lvl = 0;
	lvl = Math.min(Math.max(lvl, 0), LADDER.length - 1);
	const days = String(p.status ?? "") === "×" ? 1 : LADDER[lvl];
	return last.plus(dv.duration(days + " days"));
}

const all = dv.pages(SRC);
const active = all.where(p => p.retired !== true);
const retiredPages = all.where(p => p.retired === true);

// ===== 今日の復習キュー =====
const queue = active
	.where(p => { const d = dueOf(p); return d && d <= today; })
	.sort(p => dueOf(p), "asc");

const n = queue.length;
if (n >= QUEUE_WARN) {
	dv.el("h1", `🔥 今日の復習: ${n} 件 — 新規問題の追加は控えめに`);
} else {
	dv.el("h1", `📌 今日の復習: ${n} 件`);
}

if (n === 0) {
	dv.paragraph("今日の復習はありません 🎉");
} else {
	dv.table(
		["問題", "分野", "level", "期限", "前回判定"],
		queue.map(p => {
			const d = dueOf(p);
			const overdue = Math.round(today.diff(d, "days").days);
			return [
				p.file.link,
				p.category || "(未分類)",
				p.level ?? 0,
				d.toFormat("yyyy-MM-dd") + (overdue > 0 ? `（${overdue}日超過）` : ""),
				p.status || "（新規）",
			];
		})
	);
}

// ===== 統計 =====
dv.header(2, "📊 統計");
dv.paragraph(`回転中: **${active.length}** 問　／　卒業済み: **${retiredPages.length}** 問`);

// 分野別（出題範囲の偏りチェック用）
const byCat = new Map();
for (const p of active) {
	const c = p.category || "(未分類)";
	if (!byCat.has(c)) byCat.set(c, { levels: [], retired: 0 });
	byCat.get(c).levels.push(Number(p.level) || 0);
}
for (const p of retiredPages) {
	const c = p.category || "(未分類)";
	if (!byCat.has(c)) byCat.set(c, { levels: [], retired: 0 });
	byCat.get(c).retired++;
}
dv.header(3, "分野別");
dv.table(
	["分野", "回転中", "平均level", "卒業済み"],
	[...byCat.entries()]
		.sort((a, b) => a[0].localeCompare(b[0], "ja"))
		.map(([c, v]) => [
			c,
			v.levels.length,
			v.levels.length
				? (v.levels.reduce((a, b) => a + b, 0) / v.levels.length).toFixed(1)
				: "-",
			v.retired,
		])
);

// 直近7日の負荷予測（今日は超過分を含む）
dv.header(3, "直近7日の復習予定件数");
const WD = ["月", "火", "水", "木", "金", "土", "日"];
const rows = [];
for (let i = 0; i <= 7; i++) {
	const day = today.plus(dv.duration(i + " days"));
	const cnt = active.where(p => {
		const d = dueOf(p);
		if (!d) return false;
		return i === 0 ? d <= today : d.toISODate() === day.toISODate();
	}).length;
	rows.push([
		day.toFormat("yyyy-MM-dd") + (i === 0 ? "（今日）" : ""),
		WD[day.weekday - 1],
		cnt,
		"█".repeat(Math.min(cnt, 40)),
	]);
}
dv.table(["日付", "曜日", "件数", ""], rows);
```
