ランキングの性能評価に使う指標。
特徴としてはシンプルで分かりやすく、ビジネス側に説明がしやすい。また、Recallよりもkに一つでもあればokと評価するため寛容である。
$$
\text{HR@}k = \frac{1}{Q} \sum_{q=1}^{Q} \mathbb{1}\left\{ G_q \cap S_{q,k} \neq \emptyset \right\}
$$
- $\text{HR@}k$：ヒット率（Hit Rate at k）。上位 $k$ 件の推薦に正解が含まれた割合
- $Q$：評価対象のクエリ（ユーザー）の総数
- $q$：各クエリのインデックス（$q = 1, 2, \dots, Q$）
- $\sum_{q=1}^{Q}$：全クエリにわたる総和
- $\mathbb{1}\{\cdot\}$：指示関数。中の条件が真なら1、偽なら0を返す
- $G_q$：クエリ $q$ の正解アイテムの集合（ground truth）
- $S_{q,k}$：クエリ $q$ に対する上位 $k$ 件の推薦アイテムの集合
- $\cap$：共通部分（[[積集合]]）。両方の集合に含まれる要素
- $\emptyset$：空集合。要素が1つもない状態
- $G_q \cap S_{q,k} \neq \emptyset$：正解と推薦の共通要素が存在する（＝上位 $k$ 件に正解が1つ以上入っている）