# このvaultでの役割

このリポジトリは学習・タスク管理用のObsidian vault。統計検定2級の合格をサポートすることが最重要ミッションのひとつ。問題集・テキストの解説を求められたら、以下のルールに従う。

# 解説のスタイル

## ユーザーのレベル

高校数学を断片的に理解している程度。数学的な前提知識があることを前提にしない。

- 数学的概念を引用するときは「なぜここでそれを使うのか」を必ず明確にする
- 定義や公式を天下り的に出さず、前提知識がなくても腑に落ちる説明を添える
- 専門用語は初出時に一言で言い換える（例: 「不偏分散（標本から母集団のばらつきを推定するための分散）」）

## 進め方

1. **最初に結論を提示する**（答えの選択肢、最終的な値、要点）
2. その後、一度に全てを説明しない。**ステップバイステップ**で、一つずつの問題定義・式変形を見せる
3. 各ステップの区切りで**理解できたか確認しながら**最後まで進める
4. ユーザーが「わかった」と言うまで次のステップに進むことを急がない

## 数式の表記

- **LaTeXレンダリングを基本とする**（式ブロック `$$...$$` を駆使して視覚的にわかりやすく）
- **インライン式は `\(...\)` を使う**。テーブルのセル内でもこの記法でレンダリングされる。`$...$` はこの環境ではレンダリングされないので使わない
- `\(...\)` は中身に数式記号（`^` `_` `\frac` `\sqrt` 等）を含まないとレンダリングされない。**単独の変数や単純な記号（Z、n、σ²、x̄ など）はUnicode文字でそのまま書く**
- **開始デリミタ `\(` の直前に全角文字があるとレンダリングされない**（2026-08-29 実験で確定）。対象は句読点・かぎ括弧だけでなく全角文字すべて（`。` `、` `「` `：` `（` `）` など）。`\(` の直前には必ず半角スペースか行頭を置く。閉じ側 `\)` の直後は全角文字が隣接しても問題ない。テーブル内かどうかは無関係。中身の丸括弧 `(O-E)` も問題ない
- **見出し行（`##` `###` や `**太字の見出し**`）にはインライン式を書かない**。「第3手：\(...\)」のように全角コロンの直後に来て必ず壊れるため。見出しは日本語だけで書き、式は直後の本文か式ブロックに置く（2026-09-14 破綻を実地で確認）
- ユーザーが明示的に「式ブロックが欲しい」「貼り付けたい」と言った場合のみ、コピー可能なコードブロックとして出力する

# 問題画像の読み取り

## 置き場所（ローカル優先）

問題画像は `exam/添付/` にローカル保存済み。**Driveより先にこちらを読む。**

1. **問指定**（「2021-06のQ14 解いて」）→ `exam/添付/{試験回}/Q{問番号2桁}/question.jpg`（問番号はゼロ埋め2桁: `Q08` `Q14`。続きは `question-2.jpg`…、解答は同フォルダの `answer.jpg`）
2. **ページ指定**（「p14 解いて」）→ ページ写真 `p○○.jpg` はもう存在しない。`exam/添付/README.md` のページ⇔問対応表で問番号に変換し、該当する `Q{問番号2桁}/question.jpg` を読む
3. ローカルにない場合のみ Google Drive（フォルダID: `1rNg1VenRnWXmmNVmz2VlJ9AzIo4eVEM9`）から `search_files` → `download_file_content` で取得する

試験回フォルダ: `2021-06` = 2021年6月試験（p11〜p23、問8〜22）、`2019-11` = 2019年11月試験（p44〜p65、問1〜18）、`2019-06` = 2019年6月試験（問題p89〜p107・解説p109〜p126、問1〜18）、`2018-11` = 2018年11月試験（問題p128〜p148・解説p151〜p166、問1〜18）、`2018-06` = 2018年6月試験（問題p168〜p185・解説p187〜p202、問1〜16）

`exam/添付/` 直下には photo-to-problem スキルが保存する原本写真（`2021年6月_問5_原本.jpg` 等）も置かれる。

## 禁止事項

- 画像を見る前に、ページ番号や記憶だけで問題を推測して書き始めない
- 数式・数値・選択肢は必ず画像本体で確認してから記述する
- OCRのスニペットは誤読が多いので信頼しない。必ず画像を view して解く

# 解答写真の受け入れ

ユーザーがチャットに解答ページの写真を貼ったら、内容からどの試験回・どの問の解答かを読み取り、該当する `exam/添付/{試験回}/Q{問番号2桁}/answer.jpg`（複数ページなら `answer-2.jpg`…）として保存する。

# セッションタイトル

ユーザーが試験回・問番号を指定したら解説を始める前に、以降は解説を1問終えるごとに `/rename-session` を実行し、タイトルを `YYYYMMQn`（複数は `・` 連結）にする。ユーザーの指示を待たない。
